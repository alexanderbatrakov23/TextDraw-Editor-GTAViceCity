# 📝 TextDraw Editor v1.0 — Custom Menu

Редактор текстдравов для управления текстом, спрайтами и боксами на экране: перемещение, изменение размера, копирование, удаление и экспорт в Pawn-код.

---

## 🎮 Основное управление

| Клавиша           | Действие                                          |
|-------------------|---------------------------------------------------|
| `M`               | Открыть главное меню редактора                    |
| `1` / `2`         | Переключение между текстдравами (предыдущий / следующий) |
| `L`               | Удалить текущий выбранный текстдрав               |
| `C`               | Копировать текущий текстдрав (копия со смещением +10 пикселей) |
| `E`               | Экспортировать все текстдравы в готовый Pawn-код  |
| `N`               | Остановить режим редактирования полностью         |

---

## 📍 Режим позиционирования

| Клавиша                     | Действие                          |
|-----------------------------|-----------------------------------|
| `W` / `↑` (Стрелка вверх)   | Переместить текстдрав вверх       |
| `S` / `↓` (Стрелка вниз)    | Переместить текстдрав вниз        |
| `A` / `←` (Стрелка влево)   | Переместить текстдрав влево       |
| `D` / `→` (Стрелка вправо)  | Переместить текстдрав вправо      |

---

## 📏 Режим размера

| Клавища                     | Действие                                          |
|-----------------------------|---------------------------------------------------|
| `W` / `↑` (Стрелка вверх)   | Увеличить высоту (для спрайтов/бокса) или размер букв по Y |
| `S` / `↓` (Стрелка вниз)    | Уменьшить высоту (для спрайтов/бокса) или размер букв по Y |
| `A` / `←` (Стрелка влево)   | Уменьшить ширину (для спрайтов/бокса) или размер букв по X |
| `D` / `→` (Стрелка вправо)  | Увеличить ширину (для спрайтов/бокса) или размер букв по X |

---

## 🧊 Дополнительно в режиме редактирования

| Клавиша           | Действие                          |
|-------------------|-----------------------------------|
| `5`               | Заморозить персонажа (чтобы не мешал) |
| `6`               | Разморозить персонажа             |

---

## 📝 Примечания

- При копировании (`C`) новый текстдрав создаётся со смещением **+10 пикселей** относительно оригинала.
- Экспорт (`E`) генерирует готовый **Pawn-код** для вставки в мод.
- Клавиша `N` полностью останавливает режим редактирования — все текстдравы фиксируются.
- Режим позиционирования и режим размера работают независимо (переключение через меню `M`).

---

## 🛠️ Версия

**v1.0** — базовая функциональность TextDraw Editor.
